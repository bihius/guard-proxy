import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export function DashboardPage() {
  return (
    <PagePlaceholder
      eyebrow="Dashboard"
      title="Dashboard placeholder"
      description="This is the future home of the course dashboard: stat cards, recent activity, and quick status. For now it proves the route and app shell are wired correctly."
    />
  );
}
