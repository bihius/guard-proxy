import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export function VHostsPage() {
  return (
    <PagePlaceholder
      eyebrow="VHosts"
      title="VHosts list placeholder"
      description="This route will hold the shared entry point for the VHosts table, domain filter, and future detail navigation."
    />
  );
}
